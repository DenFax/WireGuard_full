# WireGuard Full Install

Автоматическая установка WireGuard VPN с WGDashboard панелью и Qwen Code skills.

## 🚀 Быстрый старт

### Одна команда для полной настройки:

```bash
curl -fsSL https://raw.githubusercontent.com/USERNAME/WireGuard_full/main/install.sh | sudo bash
```

**Вам будет предложено ввести:**
- GitHub username (для скачивания skills)

### С параметрами:

```bash
curl -fsSL https://raw.githubusercontent.com/USERNAME/WireGuard_full/main/install.sh | sudo bash -s -- \
  --wg-port 51820 \
  --dashboard-port 10086 \
  --domain vpn.example.com \
  --github-user YOUR_USERNAME
```

## 📋 Параметры

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--wg-port` | Порт WireGuard (UDP) | `51820` |
| `--dashboard-port` | Порт WGDashboard (TCP) | `10086` |
| `--domain` | Домен для HTTPS | — |
| `--github-user` | Ваш GitHub username | (запрос) |
| `--github-repo` | Имя репозитория | `WireGuard_full` |
| `--branch` | Ветка GitHub | `main` |
| `--no-qwen` | Не устанавливать Qwen Code | — |

## 📦 Что устанавливается

| Компонент | Описание |
|-----------|----------|
| **WireGuard** | VPN сервер |
| **WGDashboard** | Веб-панель управления |
| **Qwen Code** | AI-ассистент с skills (опционально) |
| **Nginx** | Reverse proxy для HTTPS |
| **UFW** | Брандмауэр |
| **Certbot** | SSL сертификаты Let's Encrypt |

## 🔧 После установки

### Доступ к панели

```
HTTP:  http://<IP-сервера>:10086
HTTPS: https://<ваш-домен>
```

**Учётные данные:**
- Логин: `admin`
- Пароль: `admin` ⚠️ **смените при первом входе!**

### Полезные команды

```bash
# Статус WireGuard
wg show

# Статус панели
systemctl status wg-dashboard

# Перезапуск панели
systemctl restart wg-dashboard

# Запуск Qwen Code
qwen

# Просмотр информации
cat /root/wgdashboard-info.txt
```

## 📁 Структура репозитория

```
WireGuard_full/
├── install.sh              # Главный скрипт установки
├── skills/
│   └── wireguard-vpn/
│       ├── SKILL.md        # Skill для Qwen Code
│       └── requirements.txt
└── README.md
```

## 🤖 Qwen Code Skill

Skill устанавливается в `/root/.qwen/skills/wireguard-vpn/` и включает:

- `setup_server` — установка WGDashboard
- `get_interfaces` — список интерфейсов
- `get_peers` — список клиентов
- `create_peer` — создание нового клиента
- `get_peer_config` — конфиг для подключения
- `disable_peer` — блокировка
- `delete_peer` — удаление
- `get_status` — статистика

### Примеры запросов к Qwen:

```
"Создай VPN для Иванова на 10.0.0.15"
"Заблокируй доступ Петрову"
"Покажи всех клиентов"
"Установи WGDashboard на сервер 203.0.113.50"
```

## 🔒 Требования к серверу

- **ОС:** Ubuntu 20.04+ / 22.04+ / 24.04+
- **Права:** root или sudo
- **Порты:**
  - `22/tcp` — SSH
  - `51820/udp` — WireGuard
  - `10086/tcp` — WGDashboard
  - `80/tcp` — HTTP (для Let's Encrypt)
  - `443/tcp` — HTTPS (если используется домен)

## 🛡️ Безопасность

1. **Смените пароль admin** при первом входе
2. **Настройте HTTPS** через `--domain`
3. **Ограничьте доступ** к порту 10086 по IP
4. **Используйте SSH-ключи** вместо паролей

## 📝 Лицензия

- WGDashboard: [GitHub](https://github.com/donaldzou/WGDashboard)
- Скрипты: MIT

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте логи: `journalctl -u wg-dashboard -f`
2. Проверьте WireGuard: `wg show`
3. Проверьте порты: `ufw status`
